/// Non-web stub — dart:html is unavailable on native (Android/iOS) and breaks
/// AOT compilation, so client_loader.dart binds this via conditional import.
String? readSsoFragmentToken() => null;

void clearUrlFragment() {}
