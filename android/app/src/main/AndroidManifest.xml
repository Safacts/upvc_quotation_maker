<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- IN-APP UPDATER: without this the system package installer refuses
         ACTION_VIEW installs from our app on Android 8+ (API 26+), and
         Settings.ACTION_MANAGE_UNKNOWN_APP_SOURCES (MainActivity's fallback)
         never shows an enable toggle for us. Sideload-only fleet — no Play
         policy concern. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <!-- WHITELABELING: the app name is a Gradle manifest placeholder so a
         per-client APK needs no source edit. The default in
         app/build.gradle.kts reproduces the previous hard-coded label exactly,
         so existing CI builds are unaffected. Override per client by passing
         the Gradle property vitharn.appLabel at build time (see the header
         comment in app/build.gradle.kts for the full command). -->
    <application
        android:label="${appLabel}"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- IN-APP UPDATER: registers the androidx FileProvider that
             MainActivity.handleInstall() resolves as "$packageName.fileprovider"
             to turn the downloaded APK into an installable content:// URI.
             WITHOUT this declaration FileProvider.getUriForFile() throws
             IllegalArgumentException on every update attempt — the updater
             could download but never install. res/xml/file_paths.xml (already
             present) covers getExternalStorageDirectory(). The ${applicationId}
             placeholder keeps per-client white-label ids working. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <package android:name="com.whatsapp" />
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="whatsapp"/>
        </intent>
        <package android:name="org.telegram.messenger" />
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tg"/>
        </intent>
    </queries>
</manifest>
