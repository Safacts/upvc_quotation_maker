import TeamClient from "./TeamClient";

export const metadata = {
  title: "Team & Permissions — Console",
  description: "Manage staff roles, workshop access, and sales permissions.",
};

export default function TeamPage() {
  return <TeamClient />;
}
