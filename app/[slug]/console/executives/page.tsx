import TasksClient from "../tasks/TasksClient";

export const dynamic = "force-dynamic";

// Executives surface reuses the same assignment table for MVP.
// A dedicated executive directory ships after the pilot proves demand.
export default function ExecutivesPage() {
  return <TasksClient />;
}
